# GroupBy and Merge Exercises

## Setup

```python
import pandas as pd
import numpy as np

# Customer data
customers = pd.DataFrame({
    'customer_id': [1, 2, 3, 4, 5, 6],
    'name': ['Alice', 'Bob', 'charlie', 'Diana', '  Eve  ', 'Frank'],
    'city': ['NYC', 'LA', 'nyc', 'Chicago', 'LA', 'Chicago'],
    'membership': ['Gold', 'Silver', 'gold', 'Bronze', 'Gold', None]
})

# Product data
products = pd.DataFrame({
    'product_id': ['A', 'B', 'C', 'D', 'E'],
    'product_name': ['Laptop', 'Phone', 'Tablet', 'Headphones', 'Charger'],
    'category': ['Electronics', 'Electronics', 'Electronics', 'Accessories', 'Accessories'],
    'price': [1200, 800, 400, 150, 25]
})

# Order data
orders = pd.DataFrame({
    'order_id': [101, 102, 103, 104, 105, 106, 107, 108, 109, 110],
    'customer_id': [1, 2, 1, 3, 4, 2, 5, 3, 1, 6],
    'product_id': ['A', 'B', 'C', 'A', 'D', 'E', 'B', 'B', 'E', 'C'],
    'quantity': [1, 2, 1, 1, 3, 5, 1, 2, 2, 1],
    'date': ['2024-01-15', '2024-01-16', '2024-01-16', '2024-02-01', '2024-02-10', 
             '2024-02-15', '2024-03-01', '2024-03-05', '2024-03-10', '2024-03-15']
})

# Store data
stores = pd.DataFrame({
    'city': ['NYC', 'LA', 'Chicago', 'Houston'],
    'store_name': ['Manhattan Hub', 'Hollywood Store', 'Loop Center', 'Space City Shop'],
    'employee_count': [25, 30, 20, 15]
})
```

---

## Exercise 1: Basic Cleaning
Clean the `customers` DataFrame:
- Strip whitespace from `name`
- Standardize `city` and `membership` to title case
- Fill missing `membership` values with "Basic"

---

## Exercise 2: Simple GroupBy - Orders by Customer
Using the `orders` DataFrame, find the total quantity ordered by each `customer_id`.

---

## Exercise 3: GroupBy with Aggregation
Group the `orders` by `product_id` and calculate both the total quantity sold and the number of orders for each product.

---

## Exercise 4: Basic Merge - Orders with Products
Merge the `orders` DataFrame with `products` to add product information to each order. Calculate a new column `total_price` (quantity * price).

---

## Exercise 5: GroupBy on Merged Data
Using your result from Exercise 4, find the total revenue generated by each product category.

---

## Exercise 6: Multiple Merges - Full Order Details
Create a comprehensive order view by merging `orders` with both `customers` and `products`. The result should show: order_id, customer name, product name, quantity, and total price.

---

## Exercise 7: GroupBy with Multiple Columns
Using your merged data from Exercise 6, find the total spending by each customer in each product category.

---

## Exercise 8: Merge with Different Join Types
Merge `customers` with `stores` on `city`. First use an inner join, then a left join. What's the difference in the results? Which customers don't have a local store?

---

## Exercise 9: Aggregating Customer Behavior
Create a customer summary showing for each customer:
- Their name and city
- Total number of orders
- Total quantity purchased
- Total amount spent

Sort by total amount spent (descending).

---

## Exercise 10: Monthly Revenue Report
Using the orders and products data:
1. Extract the month from the order date
2. Calculate total revenue per month
3. Identify which month had the highest revenue and which product category drove most of that revenue

---

## Exercise 11: Top Customers by Category
For each product category, find the customer who spent the most money. Display the category, customer name, and their total spending in that category.

---

## Exercise 12: City Performance Analysis
Create a report showing for each city:
- Number of customers
- Total revenue from customers in that city
- Average spending per customer
- Store name (if exists)

Include cities even if they don't have a store.
